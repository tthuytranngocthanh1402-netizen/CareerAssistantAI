# Bí mật – tuyệt đối không commit
api/config.php
.env

.claude/
.DS_Store
*.log

*.xlsx
*.xls
*.csv

# Dữ liệu trắc nghiệm ẩn danh trên server (không commit)
api/storage/*.jsonl
# Khóa ký phiên, tài khoản (mật khẩu đã băm) và lịch sử trò chuyện của người dùng (không commit)
api/storage/*.key
api/storage/accounts/
api/storage/users/

# Ghi âm giọng thật (chỉ dùng cục bộ để tạo narration.m4a)
video/voice/*
!video/voice/README.md
